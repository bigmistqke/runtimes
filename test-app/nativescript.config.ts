// This is a fake config file for `tns debug` to work with test-app

import { NativeScriptConfig } from '@nativescript/core';

export default {
  id: 'com.tns.testapplication',
  appPath: 'app',
} as NativeScriptConfig;
